import {TbFilterSearch} from 'react-icons/tb';

import {IconProps} from '@/shared/interfaces/common';

export default function FilterSearchIcon(props: IconProps) {
  return <TbFilterSearch {...props} />;
}
