import {MdOutlineArrowOutward} from 'react-icons/md';

import {IconProps} from '@/shared/interfaces/common';

export default function ArrowUpRightIcon(props: IconProps) {
  return <MdOutlineArrowOutward {...props} />;
}
