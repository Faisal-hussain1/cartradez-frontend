import {HiOutlineExclamationTriangle} from 'react-icons/hi2';
import {IconProps} from '@/shared/interfaces/common';

export default function WarningIcon(props: IconProps) {
  return <HiOutlineExclamationTriangle {...props} />;
}
