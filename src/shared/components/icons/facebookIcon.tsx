import React from 'react';
import {LuFacebook} from 'react-icons/lu';
import {IconProps} from '@/shared/interfaces/common';

export default function FacebookIcon(props: IconProps) {
  return <LuFacebook {...props} />;
}
