import React from 'react';
import {CiLinkedin} from 'react-icons/ci';
import {IconProps} from '@/shared/interfaces/common';

export default function LinkedinIcon(props: IconProps) {
  return <CiLinkedin {...props} />;
}
